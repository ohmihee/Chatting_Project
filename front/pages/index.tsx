import React from 'react'
import RootProvider from '../Providers/rootProvider'

const Home = ({children}) => {
    return (
        <>
         {children}
         home
        </>
    )
}

export default Home